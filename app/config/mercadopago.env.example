# Server only. Never use a NEXT_PUBLIC_ prefix for this credential.
MERCADOPAGO_ACCESS_TOKEN=
# Required for the official CardForm checkout. Only this public key reaches the browser.
MERCADOPAGO_PUBLIC_KEY=
# Secret generated in Your integrations > Webhooks, for the same application/environment.
MERCADOPAGO_WEBHOOK_SECRET=
# Public HTTPS callbacks; independent of the administrative origin.
MERCADOPAGO_SITE_ORIGIN=https://marquesano.com.br
# Existing DATABASE_URL must point to the administrative PostgreSQL database.
